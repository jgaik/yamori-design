{
  "name": "@yamori-design/styles",
  "version": "0.4.1",
  "description": "Yamori Design stylesheets files",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "npm run clean && sass --no-source-map src:dist",
    "dev": "sass --no-source-map --watch src:dist",
    "clean": "rm -rf .turbo && rm -rf dist"
  },
  "author": "Jakub Gaik",
  "license": "MIT",
  "devDependencies": {
    "sass": "^1.80.3"
  }
}
